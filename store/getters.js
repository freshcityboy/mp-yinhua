export default {
  // categorysArr (state) { // 将1数组 转换为2维数组
  //   let categorys = state.categorys
  //   let Arr = []
  //   let arr = []
  //   categorys.forEach(item => {
  //     if (arr.length === 8) {
  //       arr = []
  //     }
  //     // 先将arr 的空数组放到Arr中，然后在给arr添加项，当arr.length 大于 7 时，再将arr清空后，再推一个空arr到Arr中（基本原理：先推数组在填充）
  //     if (arr.length === 0) {
  //       Arr.push(arr)
  //     }
  //     arr.push(item)
  //   })
  //   return Arr
  // },
  // foodCount (state) {
  //   return state.cartFoods.reduce((pre, cur) => pre + cur.count, 0)
  // },
  // totalPrice (state) {
  //   return state.cartFoods.reduce((pre, cur) => pre + cur.count * cur.price, 0)
  // }
	
}
